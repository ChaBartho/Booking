server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:6969/booking
    username: postgres
    password: pass
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create